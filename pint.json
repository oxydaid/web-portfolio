{
    "preset": "laravel",
    "rules": {
        "no_unused_imports": true,
        "ordered_imports": {
            "sort_algorithm": "alpha"
        }
    },
    "exclude": [
        "node_modules",
        "vendor",
        "storage",
        "bootstrap/cache"
    ]
}
